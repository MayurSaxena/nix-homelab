---
- name: Start the guest agent
  ansible.builtin.systemd_service:
    name: qemu-guest-agent
    enabled: true
    state: started
  become: true


- name: Reload sshd
  ansible.builtin.systemd_service:
    name: ssh
    state: reloaded
  become: true
